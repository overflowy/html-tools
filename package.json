{
  "name": "html-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "check": "tsc --noEmit",
    "test": "bun run smoke.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "playwright-core": "^1.62.1",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "lz-string": "^1.5.0"
  }
}
