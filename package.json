{
  "name": "html-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
